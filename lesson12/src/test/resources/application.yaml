# spring properties
spring:
  main:
    banner-mode: "off"
    allow-bean-definition-overriding: true
  #
  shell:
    interactive:
      enabled: false
  data:
    mongodb:
      authentication-database: admin
      #username: igorr
      #password:
      database: testdb
      host: localhost
      port: 27017
      #uri: mongodb+srv://igorr:password123@cluster0-x1evu.gcp.mongodb.net

# app properties
messages:
  language: "en"
  country: "GB"
  bundle: "/messages/app_message"
# DB access
mongodb:
  host: "mongodb+srv://%s:%s@cluster0-x1evu.gcp.mongodb.net/%s"
  dbname: testdb
  username: igorr
  password: password123

