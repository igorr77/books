spring:
  main:
    banner-mode: "off"
    allow-bean-definition-overriding: true
  #
  shell:
    interactive:
      enabled: false
  data:
    mongodb:
      authentication-database: admin
      #username: igorr
      #password: 9FnEH8OfZR6VKKio
      database: testdb
      host: localhost
      port: 27017
      #uri: mongodb+srv://igorr:9FnEH8OfZR6VKKio@cluster0-x1evu.gcp.mongodb.net


  h2:
    console:
      enabled: true

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  batch:
    initialize-schema: always
  datasource:
  #driver-class-name: org.h2.Driver
  #url: jdbc:h2:mem:testdb
  #username: sa
  #password:
  #data: testdata.sql
  #driver-class-name: mongodb.jdbc.MongoDriver


#
messages:
  language: "en"
  country: "GB"
  bundle: "/messages/app_message"
# DB access
mongodb:
  host: "mongodb+srv://%s:%s@cluster0-x1evu.gcp.mongodb.net/%s"
  dbname: testdb
  username: igorr
  password: DfczGegrby123

pool:
  jdbcDriver: org.postgresql.Driver
  jdbcString: jdbc:postgresql://localhost:5432/otus
  jdbcUser: postgres
  jdbcPassword: postgres
